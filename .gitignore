# Project place file
/rng-tapping-game.rbxlx

# Roblox Studio lock files
/*.rbxlx.lock
/*.rbxl.lock

sourcemap.json

# Wally packages — restored with `wally install`.
# Commit wally.toml + wally.lock; the downloaded packages stay out of Git.
/Packages/
/ServerPackages/
/DevPackages/
